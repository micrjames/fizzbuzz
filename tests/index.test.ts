import { fizzbuzz } from "../fizzbuzz";

describe("Fizzbuzz", () => {
   beforeAll(() => {
   });
   describe("Listed", () => {
	  test.todo("Should be fizz at the specified positions.");
	  test.todo("Should be buzz at the specified positions.");
	  test.todo("Should be fizzbuzz at the specified positions.");
	  test.todo("Should be equal to the array.");
   });
});
